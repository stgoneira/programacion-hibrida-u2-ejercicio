import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u2.ej3',
  appName: 'ph-u2-ej3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
